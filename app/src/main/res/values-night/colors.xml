<?xml version="1.0" encoding="utf-8"?>
<resources>
    <color name="button_background">@color/button_background_dark</color>

    <!-- Member role (Owner/Admin) - dark mode -->
    <color name="member_role_color">@color/button_green</color>

    <!-- Settings accent (dark mode): keep button green -->
    <color name="settings_accent">@color/button_green</color>

    <!-- FAB colors (dark mode) -->
    <color name="fab_bg">@color/black</color>
    <color name="fab_icon">@color/white</color>

    <!-- Chat bubble colors (dark mode) -->
    <color name="chat_bubble_me_bg">#005C4B</color>
    <color name="chat_bubble_other_bg">#2A2A2A</color>
    <color name="chat_bubble_me_text">#FFFFFF</color>
    <color name="chat_bubble_other_text">#FFFFFF</color>
    <color name="chat_bubble_meta_text">#B0B0B0</color>

    <!-- Chat composer (dark mode) - keep existing green icons -->
    <color name="chat_composer_icon_tint">@color/button_green</color>

    <!-- Popup (message options) -->
    <color name="popup_icon_tint">@color/white</color>
    <color name="popup_divider">#3A3A3A</color>
    <color name="popup_stroke">#3A3A3A</color>

    <!-- Reply preview -->
    <color name="reply_header_text">@color/white</color>
</resources>